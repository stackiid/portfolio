// =========================================================
//                  COUNTERS / STATISTICS
// ---------------------------------------------------------
// These are shown in the hero's experience badge. Keep them
// honest - update the number as it becomes true, don't round
// up. "years" accepts a string ("1+") because experience is
// rarely a clean integer.
// =========================================================

const counters = {
  yearsExperience: "1+",
  projectsShipped: 0,
  happyClients: 0,
};

export default counters;
